node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.local
coverage/
.vite/
.turbo/
*.tsbuildinfo

# Daemon axiomd — base SQLite locale (cache/persistance), jamais committée
apps/daemon/axiom.db
apps/daemon/axiom.db-shm
apps/daemon/axiom.db-wal

# Scratch de suivi de progression (subagent-driven-development) — jamais committé
.superpowers/

# Captures Chrome DevTools / Playwright MCP (vérifications runtime)
.playwright-mcp/

# Artefacts Playwright e2e (rapports, traces, résultats) — jamais committés
apps/web/test-results/
apps/web/playwright-report/
apps/web/.playwright/

# Venv Python de l'oracle golden (pandas-ta-classic) — jamais committé, voir scripts/golden/README.md
scripts/golden/.venv/

# Cache de klines Binance de la campagne de backtest — données brutes, jamais committées
scripts/.cache-klines/
