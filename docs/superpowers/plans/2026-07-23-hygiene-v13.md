# Hygiène v1.3 — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fixes sécurité restants (sync exchanges, token Telegram), test du registre indicateurs + rangement, doc de lecture cvdSpotPerp — spec `2026-07-23-lot-v13-consolidation-quickwins-design.md` (branche 1).

**Architecture:** Trois tâches indépendantes et chirurgicales ; aucune nouvelle feature, uniquement de la dette cadrée par les revues des 22-23/07.

**Tech Stack:** TypeScript, vitest.

## Global Constraints

- Commentaires en **français**. Modifications chirurgicales — chaque ligne trace à la spec.
- TDD sur toute logique pure nouvelle. `git -C` systématique.
- Branche : `chore/hygiene-v13`. Gate : `pnpm test` racine + `pnpm typecheck` verts.

---

### Task 1: Fixes sécurité — sync exchanges & rédaction token Telegram

**Files:**
- Modify: `apps/web/src/store/sync.ts` (+ le module qui fait autorité sur la liste — chercher la liste des 9 exchanges autorisés dans `apps/web/src/store/persist.ts` ~:91)
- Test: compléter le test de sync existant (ou en créer un)
- Modify: `apps/daemon/src/notify.ts`
- Test: `apps/daemon/src/notify.test.ts` (créer si absent — chercher comment les tests daemon existants sont structurés)

**Interfaces (Produces):**
```ts
// UNE source de vérité : exporter la liste autorisée depuis son site d'autorité (persist.ts ou types),
// sync.ts l'importe — plus de copie locale. Test : la validation sync accepte un message bybit/okx/
// hyperliquid/synthetic et refuse un exchange inconnu.
// notify.ts : export function redigerErreurTelegram(err: unknown): string
//   — String(err) avec motif /\/bot[^/\s]+/g remplacé par "/bot***" ; le console.error l'utilise.
```

- [ ] **Step 1: Tests rouges** — (a) sync : message d'un exchange de la liste complète accepté (aujourd'hui rejeté pour bybit — le test DOIT être rouge avant fix), exchange inconnu rejeté ; (b) `redigerErreurTelegram` : une Error dont le message contient `https://api.telegram.org/bot123:ABC/sendMessage` ressort avec `/bot***/`, un err non-Error est stringifié, un message sans token est inchangé.
- [ ] **Step 2: Rouge → Step 3: Implémenter → Step 4: Vert** — suites web ET daemon.
- [ ] **Step 5: Commit** — `fix(securite): liste exchanges sync unifiée + token Telegram expurgé des logs`

### Task 2: Registre indicateurs — test, commentaires, rangement

**Files:**
- Create: `packages/indicators/src/registry.test.ts`
- Modify: `packages/indicators/src/registry.ts` (commentaires)
- Rename: `packages/indicators/src/volume/cvd.ts` → `packages/indicators/src/orderflow/cvd.ts` (+ son `.test.ts`, via `git mv`)

- [ ] **Step 1: Test rouge partiel** — `registry.test.ts` : `INDICATORS.length === 150` ; unicité des `id` (Set) ; chaque def a une `category` de l'enum et ≥ 1 output ; `getIndicator("cvd")` résout. (Le test passe dès création sauf s'il révèle un vrai écart — c'est un test de documentation, l'assertion de compte est le point.)
- [ ] **Step 2:** Commentaires de zones du registry : retirer les NOMBRES périmés (`— trend (27) —` → `— trend —`), garder les noms ; corriger le commentaire d'en-tête d'imports « fichiers sous volume/ » (inexact depuis cvdSpotPerp).
- [ ] **Step 3:** `git mv` de cvd.ts + cvd.test.ts vers `orderflow/`, ajuster les imports (registry + tout consommateur — `rg "volume/cvd"`). AUCUN changement de contenu hors chemins d'import.
- [ ] **Step 4:** `pnpm --filter @axiom/indicators test` complet vert + suite web verte (imports app éventuels).
- [ ] **Step 5: Commit** — `chore(indicators): test de registre, commentaires assainis, cvd rangé sous orderflow/`

### Task 3: Doc de lecture cvdSpotPerp + gate

**Files:**
- Modify: `packages/indicators/src/orderflow/cvdSpotPerp.ts` (description du def)

- [ ] **Step 1:** Vérifier le champ description de `IndicatorDef` (chercher comment `IndicatorMenu`/la palette affichent l'aperçu d'un indicateur — le def `cvdSpotPerp` a-t-il un champ descriptif lu par l'UI ? sinon, docstring du fichier + aperçu de la commande palette générée). Ajouter la doc : « Jambe perp : timeframes 1m-1M (Binance USDT-M) ; ailleurs CVD spot seul. Séries normalisées en écarts-types de flux — lire croisements et signe de l'histogramme, pas les niveaux. » à l'endroit réellement affiché à l'utilisateur ; si aucun canal UI n'existe, docstring seule + note dans le rapport.
- [ ] **Step 2:** `pnpm test` racine vert + `pnpm typecheck` propre (gate de branche).
- [ ] **Step 3: Commit** — `docs(indicators): lecture et limites de cvdSpotPerp exposées à l'utilisateur`
