# MC-BT — Monte-Carlo sur le backtest — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rééchantillonnage Monte-Carlo des trades du backtest (cône de percentiles d'equity + stats de risque) dans la fenêtre BT existante — spec `2026-07-23-netliq-mcbt-cot-disagg-design.md` §2.

**Architecture:** Calcul pur dans `packages/backtest` (RNG seedable INJECTÉ), section UI dans BacktestWindow (pas de nouvelle fenêtre), zéro nouvelle donnée.

**Tech Stack:** TypeScript, canvas 2D, vitest.

## Global Constraints

- Commentaires **français**. `git -C` systématique.
- **RNG injecté obligatoire** : générateur seedable type mulberry32 (seed défaut 42), JAMAIS `Math.random()` dans la logique testée — tests déterministes.
- `nChemins` défaut 500, max 2000. Seuil de validité : < 10 trades → bouton grisé avec le seuil affiché (« Monte-Carlo (≥ 10 trades) »).
- Pas de nouvelle fenêtre ni mnémonique. Zéro nouvelle source de données.
- Branche : `feat/mc-bt`. TDD. Gate : `pnpm test` racine + tsc verts + gate visuel (contrôleur).

**Modèles à lire AVANT d'implémenter :**
- `packages/backtest/src/types.ts` + `engine.ts` (forme EXACTE de la liste de trades et de l'equity : noms de champs PnL — Task 1 les consigne, le plan ne les invente pas)
- `apps/web/src/store/backtest.ts:257-329` (`BacktestState`, phase `done`, où vivent trades/equity côté store)
- `apps/web/src/components/BacktestWindow.tsx` (canvas equity existant, structure des sections)

---

### Task 1: Calcul pur `monteCarloTrades` (packages/backtest)

**Files:**
- Create: `packages/backtest/src/monteCarlo.ts` — Test: `packages/backtest/src/monteCarlo.test.ts`
- Modify: `packages/backtest/src/index.ts` (export)

**Interfaces (Produces — consommé par Task 2):**
```ts
export function mulberry32(seed: number): () => number; // RNG déterministe [0,1)
export interface ResultatMonteCarlo {
  equityFinale: { p5: number; p25: number; p50: number; p75: number; p95: number };
  maxDrawdown: { p50: number; p95: number };        // en fraction du capital initial (0.25 = −25 %)
  probRuine: number;                                 // part des chemins finissant < 0
  cheminsPercentiles: { p5: number[]; p50: number[]; p95: number[] }; // equity cumulée par pas (longueur = nb trades) pour le cône
}
export function monteCarloTrades(pnls: number[], nChemins: number, rng: () => number, capitalInitial: number): ResultatMonteCarlo | null;
// rééchantillonnage AVEC remise des PnL par trade ; null si pnls.length < 10 ;
// nChemins borné à [1, 2000] ; percentiles par interpolation linéaire (convention à commenter).
```

- [ ] **Step 1:** Consigner dans le rapport la forme exacte des trades/equity de l'engine (champs PnL, capital initial) — l'UI Task 2 s'y branche.
- [ ] **Step 2: Tests rouges** — mulberry32 : même seed → même séquence, valeurs dans [0,1) ; monteCarloTrades : déterminisme à seed fixe (snapshot de valeurs) ; PnL tous identiques → cône plat, p5=p95, maxDrawdown correct ; PnL tous positifs → probRuine 0 ; mélange gagnants/perdants → p5 < p50 < p95 (fixture commentée) ; < 10 trades → null ; nChemins clampé.
- [ ] **Step 3-5: Rouge → implémentation → vert** — `pnpm --filter @axiom/backtest test -- monteCarlo`
- [ ] **Step 6: Commit** — `feat(backtest): monteCarloTrades — rééchantillonnage des PnL avec RNG seedable injecté`

### Task 2: Section Monte-Carlo dans BacktestWindow

**Files:**
- Modify: `apps/web/src/components/BacktestWindow.tsx` (+ `apps/web/src/store/backtest.ts` si l'état MC doit y vivre — suivre la convention de la fenêtre)

**Interfaces:** Consumes `monteCarloTrades`/`mulberry32` (Task 1), trades du `backtestStore` (phase `done`).

- [ ] **Step 1:** Section sous l'equity : bouton « Monte-Carlo (500) » (grisé si < 10 trades, seuil affiché ; désactivé si run BT en cours), calcul synchrone au clic (500×N trades = négligeable ; si > ~50 ms mesuré, passer nChemins à un `Segmente` 200/500/2000 — décision consignée).
- [ ] **Step 2:** Cône p5/p50/p95 superposé au canvas equity existant (remplissage translucide token, ligne p50) OU pane canvas dédié sous l'equity si la superposition complique l'échelle X (les trades MC sont indexés par n° de trade, pas par temps — si l'equity existante est temporelle, le pane séparé est le bon choix ; consigner la décision).
- [ ] **Step 3:** Tableau compact : equity finale p5/p50/p95, max drawdown p95, prob. ruine (%) — teintes up/down, `title` natifs explicatifs. Invalidation : nouveau run BT → résultat MC effacé.
- [ ] **Step 4:** `pnpm test` racine + tsc verts. **Step 5: Commit** — `feat(backtest): section Monte-Carlo dans la fenêtre BT (cône + stats de risque)`

Gate visuel (contrôleur) : run BT builtin ≥ 10 trades → MC → cône plausible englobant l'equity réalisée, stats cohérentes (p5 ≤ p50 ≤ p95), re-run BT → MC effacé, < 10 trades → bouton grisé.
