# NETLIQ — Liquidité nette Fed — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fenêtre `NETLIQ` — net liquidity = WALCL − WTREGEN − RRPONTSYD (FRED, 2 ans) — spec `2026-07-23-netliq-mcbt-cot-disagg-design.md` §1.

**Architecture:** Réutilisation exacte du chemin FRED existant (`data/macro/fred.ts`, provider M2), calcul pur d'alignement par date avec LOCF (niveaux), store patron squeeze/cbprem (erreur non destructive), fenêtre canvas patron macro.

**Tech Stack:** TypeScript, canvas 2D, Zustand vanilla, vitest.

## Global Constraints

- Commentaires **français**. Tokens couleur au dessin, DPR, ResizeObserver. `git -C` systématique.
- ⚠️ INTERDIT d'inventer les formats FRED : la Task 1 VÉRIFIE live les 3 séries (ids, unités, forme de réponse) via le chemin M2 existant et consigne les résultats dans son rapport. Les unités FRED diffèrent par série (WALCL en millions $, à confirmer) — normaliser en **milliards $**.
- Erreur d'une des 3 jambes → message SANS masquer une courbe déjà chargée (patron cbprem/SQZ). Cache TTL 12 h (données quotidiennes). Pas de polling : chargement à l'ouverture + Rafraîchir.
- Fenêtre : id `netliq`, mnémonique `NETLIQ`, `nouveau: true` — comptes windowManager.test à jour (patron CBPREM exact).
- Branche : `feat/netliq`. TDD sur le calcul pur. Gate : `pnpm test` racine + tsc verts + gate visuel (contrôleur).

**Modèles à lire AVANT d'implémenter :**
- `apps/web/src/data/macro/fred.ts` (`createFredM2Provider` :79 — URL, clé API, parseur, gestion d'absence de clé) et `apps/web/src/store/macro.ts` (où vit la clé FRED)
- `apps/web/src/data/cbprem.ts` + `store/cbprem.ts` + `components/CbpremWindow.tsx` (patron complet le plus récent : calcul pur / store non destructif StrictMode-safe / canvas)
- `apps/web/src/store/windowManager.ts` + `commands/windowPanels.ts` + `App.tsx` (greffes fenêtre)

---

### Task 1: Vérification live FRED + fetch des 3 séries

**Files:**
- Create: `apps/web/src/data/netliq.ts` (partie fetch : `fetchSerieFred(seriesId, observationStart)` — factorisation du chemin M2 si `createFredM2Provider` n'est pas réutilisable tel quel pour une série arbitraire ; sinon le réutiliser)

**Interfaces (Produces — consommé par Task 2/3):**
```ts
export interface PointFred { date: string; valeur: number; } // date ISO "YYYY-MM-DD", valeur en Md$ APRÈS normalisation
export async function fetchSeriesNetliq(nowMs: number): Promise<{ walcl: PointFred[]; tga: PointFred[]; rrp: PointFred[] }>;
// observation_start = nowMs − 2 ans ; unités normalisées en milliards $ (facteurs consignés en commentaire avec la preuve)
```

- [ ] **Step 1: Vérifier live** (curl via le proxy dev ou le chemin fred.ts) : `WALCL`, `WTREGEN`, `RRPONTSYD` — existence, fréquence, unités (`units` dans la réponse), points `"."` (valeurs manquantes FRED). CONSIGNER dans le rapport. Si un id est faux, trouver le bon sur fred.stlouisfed.org et le documenter.
- [ ] **Step 2:** Implémenter le fetch (réutilise URL/clé/parseur du chemin M2 ; valeurs `"."` écartées). Pas de test réseau (convention repo) ; extraire et tester la normalisation d'unités si non triviale.
- [ ] **Step 3: Commit** — `feat(netliq): fetch des trois séries FRED (WALCL, TGA, RRP) via le chemin M2`

### Task 2: Calcul pur `serieNetliq`

**Files:**
- Create: `apps/web/src/data/netliq.ts` (suite) — Test: `apps/web/src/data/netliq.test.ts`

**Interfaces (Produces):**
```ts
export interface PointNetliq { date: string; netliq: number; } // Md$
export function serieNetliq(walcl: PointFred[], tga: PointFred[], rrp: PointFred[]): PointNetliq[];
// axe des dates = union des dates quotidiennes de tga/rrp ; chaque jambe forward-fillée (LOCF)
// depuis sa dernière valeur connue (WALCL hebdo → LOCF légitime, NIVEAU) ; un point n'est émis
// qu'une fois les 3 jambes amorcées ; netliq = walcl − tga − rrp ; sortie triée chrono.
export function statsNetliq(serie: PointNetliq[]): { courant: number | null; delta4s: number | null; min2a: number | null; max2a: number | null };
// delta4s = courant − valeur au dernier point ≥ 28 jours avant le dernier t (null si historique trop court).
```

- [ ] **Step 1: Tests rouges** — alignement LOCF (WALCL hebdo étalé sur les jours), amorce (aucun point avant que les 3 jambes existent), soustraction exacte sur fixture commentée, dates en désordre tolérées, delta4s aux bornes, série vide → stats null.
- [ ] **Step 2-4: Rouge → implémentation → vert** — `pnpm --filter @axiom/web test -- netliq`
- [ ] **Step 5: Commit** — `feat(netliq): série de liquidité nette et stats (purs)`

### Task 3: Store `store/netliq.ts`

**Files:**
- Create: `apps/web/src/store/netliq.ts`

**Interfaces (Produces):** patron cbpremStore : `{ enCours, serie, stats, erreur, majTs, run() }` ; run = fetchSeriesNetliq → serieNetliq → statsNetliq ; erreur posée SANS vider `serie` (effacée au seul succès) ; garde currentRunId ; cache TTL 12 h en mémoire (pas de re-fetch si < 12 h, Rafraîchir force).

- [ ] **Step 1:** Implémenter (logique de TTL extraite pure et testée si > 3 lignes).
- [ ] **Step 2:** Suite web verte. **Step 3: Commit** — `feat(netliq): store et collecte FRED (TTL 12 h, erreur non destructive)`

### Task 4: Fenêtre + enregistrement + gate

**Files:**
- Create: `apps/web/src/components/NetliqWindow.tsx`
- Modify: `apps/web/src/store/windowManager.ts`, `App.tsx`, `commands/windowPanels.ts`, `windowManager.test.ts` (comptes 26 → 27, patron CBPREM exact)

- [ ] **Step 1:** Canvas courbe 2 ans (patron CbpremWindow : DPR, tokens, tooltip paddings partagés), repères min/max 2 ans discrets (lignes pointillées + étiquettes), en-tête : badge courant (Md$), delta 4 sem (up/down), Rafraîchir (désactivé si enCours), bandeau erreur non destructif, `NoteSource` « FRED · WALCL − TGA − RRP · quotidien » + fraîcheur, run auto StrictMode-safe.
- [ ] **Step 2:** Greffes + comptes. `pnpm test` racine + tsc verts.
- [ ] **Step 3: Commit** — `feat(netliq): fenêtre NETLIQ (courbe 2 ans + delta 4 semaines)`

Gate visuel (contrôleur) : courbe plausible (~5 500-6 500 Md$ en 2026, à vérifier contre FRED), badge/delta cohérents, Rafraîchir, erreur non destructive (clé FRED absente → message sans perte).
